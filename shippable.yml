language: none

build:
  ci:
    - echo 'CI Running'
    - export IMAGE_NAME=ship-dtr.in/vishnu/dv
    - ./build.sh

  post_ci:
    - ./push.sh

integrations:
  hub:
    - integrationName: "shipdeploy-dtr"
      type: Docker Trusted Registry
      branches:
        only:
          - dtr
